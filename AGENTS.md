# Fresh Agent Instructions

Before changing this repository, read [.agents/README.md](./.agents/README.md) and the documents it routes to.

The `.agents/` directory is the project source of truth for development rules, architecture, design language, current capabilities, and verification expectations. If implementation and documentation diverge, inspect the code and configuration, then update the relevant `.agents/` document in the same change.
